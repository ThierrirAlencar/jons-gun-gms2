

image_speed = 0;
image_index = irandom(image_number-1)

life = 1; 
breakeable = true

//Properties
flamable = false; //defines either the object can be set on fire 
explosive = false; //defines either the object can explode on hit
explosion_radious = 0;//explosion radiouns on the scale of the explodion (ex: 2, 1.5, 3 and etc);
can_hold_a_bullet = true;//

